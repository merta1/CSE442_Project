package edu.buffalo.cse442.handlers;

public class SwitchHandler
{
    public int isUserpartofDebate()
    {
        //If user is part of debate return 1 else return 0;
        return 0;
    }
    public int  returnUserDebate()
    {
        //Return if the User has agreed or disagreed to the debate so that the other side can be made read only on react side
        return 0;
    }
}
